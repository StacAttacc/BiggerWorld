{
    window.commands = [
        {
            criteria = { title = "kitty-launcher"; };
            command  = "floating enable, resize set 420 195, move position center";
        }
        {
            criteria = { title = "kitty-control"; };
            command  = "floating enable, resize set 420 195, move position center";
        }
    ];
}
