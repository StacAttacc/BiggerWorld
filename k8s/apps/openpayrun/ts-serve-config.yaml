apiVersion: v1
kind: ConfigMap
metadata:
  name: ts-serve-config
  namespace: openpayrun
data:
  serve-config.json: |
    {
      "TCP": { "443": { "HTTPS": true } },
      "Web": {
        "openpayrun-api.tail789d60.ts.net:443": {
          "Handlers": { "/": { "Proxy": "http://127.0.0.1:8080" } }
        }
      },
      "AllowFunnel": { "openpayrun-api.tail789d60.ts.net:443": true }
    }
