apiVersion: v1
kind: Service
metadata:
  name: grafana-tailscale
  namespace: monitoring
spec:
  selector:
    app.kubernetes.io/name: grafana
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP 
