apiVersion: v1
kind: Service
metadata:
  name: sewing-assistant
  namespace: sewing-assistant
spec:
  selector:
    app: sewing-assistant
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
