apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prose
  namespace: prose
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - prose-api
  defaultBackend:
    service:
      name: prose
      port:
        number: 8080
