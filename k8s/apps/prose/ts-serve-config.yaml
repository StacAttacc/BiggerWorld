apiVersion: v1
kind: ConfigMap
metadata:
  name: ts-serve-config
  namespace: prose
data:
  serve-config.json: |
    {
      "TCP": { "443": { "HTTPS": true } },
      "Web": {
        "prose-api.tail789d60.ts.net:443": {
          "Handlers": { "/": { "Proxy": "http://127.0.0.1:8080" } }
        }
      },
      "AllowFunnel": { "prose-api.tail789d60.ts.net:443": true }
    }
